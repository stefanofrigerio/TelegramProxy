{
  "name": "api-proxy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "claude": "./launch-claude.sh"
  },
  "dependencies": {
    "ws": "^8"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
